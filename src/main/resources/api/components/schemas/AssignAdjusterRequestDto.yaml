type: object
description: "Request body for assigning an adjuster to a claim"
properties:
  employeeId:
    type: integer
    format: int64
    description: "The ID of the employee to be assigned as the adjuster"
required:
  - employeeId